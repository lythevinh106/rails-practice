<%= form_with(model: admin_product_order) do |form| %>
  <% if admin_product_order.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_product_order.errors.count, "error") %> prohibited this admin_product_order from being saved:</h2>

      <ul>
        <% admin_product_order.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
